To create the centos-5.7-x86_64.iso run the following command from this directory:

mkisofs -o centos-5.7-x86_64.iso -b isolinux.bin -c boot.cat -no-emul-boot -boot-load-size 4 -boot-info-table -R -J -v -T isolinux
